enum Type {
	ELECTRIC, GRASS
}

// enums are basically named integers

// with pokemon types stored as strings, using "electric" or "Lightning" instead
// of "Electric" would cause problems, so enums offer more standardization

// search up "enum bitmasks" for fancier ways of using em
